# Admin Operations Module (Phase 2 & 9)

## Responsibilities
- Curriculum authoring APIs (Courses, Lessons, Sentences, Stories).
- Platform operations dashboard:
  - Active learners count (DAL / MAL).
  - STT & LLM provider cost telemetry.
  - BullMQ queue throughput, error counts, and DLQ manual retries.
- User management:
  - Account search, manual quota overrides, role elevation.
- PDF ingestion supervisor:
  - Inspect OCR quality, re-trigger failed parse jobs.

